# Frontend build-time configuration. Copy to frontend/.env and edit.
# Only variables prefixed with VITE_ are exposed to the built bundle.

# SSO — must match the backend's SSO_* values.
VITE_SSO_ENABLED=false
VITE_SSO_AUTH_ORIGIN=https://auth.example.com
VITE_SSO_PROVIDER_NAME=SSO
