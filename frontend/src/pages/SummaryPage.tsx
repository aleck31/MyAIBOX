import SummaryProcessor from '../components/SummaryProcessor'

export default function SummaryPage() {
  return (
    <div style={{ height: '100%', display: 'flex', flexDirection: 'column' }}>
      <SummaryProcessor />
    </div>
  )
}
