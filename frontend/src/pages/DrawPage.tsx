import DrawProcessor from '../components/DrawProcessor'

export default function DrawPage() {
  return (
    <div style={{ height: '100%', display: 'flex', flexDirection: 'column' }}>
      <DrawProcessor />
    </div>
  )
}
